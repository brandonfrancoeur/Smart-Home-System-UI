package ca.uvic.seng330.assn3;

import org.junit.Test;
import ca.uvic.seng330.assn3.devices.*;

import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertSame;
import static org.junit.Assert.assertTrue;

public class NewTests {
    /*
    Hub mediator = new Hub();
    Camera c1 = new Camera(mediator);
    Thermostat t1 = new Thermostat(mediator);
    Lightbulb l1 = new Lightbulb(mediator);
    SmartPlug s1 = new SmartPlug(mediator);

    @Test
    public void testSmartPlugDefault() {
        assert !s1.getState();
    }

    @Test
    public void testrecored() throws CameraFullException {
        assertFalse("Expected to be false ", c1.getisRecording());
        c1.record();
        assertTrue("Expected to be True", c1.getisRecording());
    }

    @Test
    public void testSetTemp() throws Temperature.TemperatureOutofBoundsException {
        t1.setTemp(new Temperature(1000, Temperature.Unit.CELSIUS));
    }

    @Test
    public void testDevices() {
        Device d = new Camera(mediator);
    }

    // test on off function
    @Test
    public void testSmartPlugtoggle() {
        assertSame(s1.getState(), false);
        s1.toggle();
        assertSame(s1.getState(), true);
        s1.toggle();
        assertSame(s1.getState(), false);
    }

    @Test
    public void testlighPlugtoggle() throws CameraFullException, Temperature.TemperatureOutofBoundsException {

        // System.out.println(c1.isRecording());
        assertTrue(c1.getisRecording() == false);
        c1.record();
        // System.out.println(c1.isRecording());
        assertTrue(c1.getisRecording() == true);
        //System.out.println(c1.getNumPic());
        assertTrue(c1.getDiskSize() == 1);

        t1.setTemp(new Temperature(1000, Temperature.Unit.CELSIUS));
    }


*/
}
